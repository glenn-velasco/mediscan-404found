apiVersion: 1

providers:
  - name: mediscan
    orgId: 1
    folder: MediSCAN
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
