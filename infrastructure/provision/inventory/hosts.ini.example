# Documentation only - the real inventory is generated per-run by
# .github/workflows/provision.yml from its `host` input, not committed here.
[mediscan]
203.0.113.10 ansible_user=root ansible_ssh_private_key_file=~/.ssh/mediscan_bootstrap
